version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: report-micro-app
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
